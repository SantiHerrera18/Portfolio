---
const { name, img } = Astro.props;
console.log(img);
---

<div class="flex flex-col items-center justify-center p-2">
  <div
    style={`background-image: linear-gradient(transparent, rgba(0, 0, 0, 1)),
                  url("${img}")`}
    class="w-[100%] h-[250px] bg-cover bg-center rounded-lg shadow-xl flex flex-col justify-end p-2"
  >
    <h3 class="font-semibold text-center">{name}</h3>
  </div>
</div>
